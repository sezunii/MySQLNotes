-- brings all(*) the data from the users table
SELECT * FROM users;

-- brings the names data from the users table
SELECT name FROM users;

-- brings the id's and emails data from the users table
SELECT id, email FROM users;

-- * -> all
-- , -> more than one
-- attribute -> an attribute from the table
